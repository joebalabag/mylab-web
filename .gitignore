node_modules
dist
dist-ssr
dev-dist
*.local
.vscode
.idea
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Vite generates these when config uses ESM — never worth committing.
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Local Claude/agent scratch dir
.claude/
# Playwright artifacts
test-results/
playwright-report/
# Env — treat every .env file as secret; only .env.example is committed.
.env
.env.*
!.env.example
